version: 2

sources:
  - name: sample_data
    database: sf_dbt
    schema: dbt_sandbox
    tables:
      - name: de_info
        description: checking how ddl,schema_evolution works

models:
  - name: my_first_dbt_model
    description: "A starter dbt model"
    columns:
      - name: id
        description: "The primary key for this table"
        tests:
          - unique
          - not_null

  - name: IT_employees
    description: "employees information"
    columns:
      - name: name
        description: "The primary key for this table"
        tests:
          - unique
          - not_null
